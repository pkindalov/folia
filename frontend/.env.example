# Optional API base URL override (e.g. a deployed backend).
# Leave unset for local dev: the Vite proxy forwards /api to the backend,
# which also makes the app work from phones on the LAN.
# VITE_API_URL=http://localhost:1337
